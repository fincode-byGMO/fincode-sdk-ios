# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FincodeSDK' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FincodeSDK
  # 以下バージョンのソースをfincode > Libraryに配置して取り込んでいるためコメントアウト
  # pod 'SwiftyJSON', '5.0.0'
  # pod 'Alamofire', '4.9.1'

  target 'FincodeSDKTests' do
    # Pods for testing
  end

end
